import { hashCanonical } from "./canonical.js"; import { parseUnifiedDiff } from "./parser.js"; import { runRules } from "./rules.js"; import type {PatchLensReview,ReviewOptions} from "./types.js";
export function reviewDiff(diff:string,options:ReviewOptions={}):PatchLensReview{const patch=parseUnifiedDiff(diff);const findings=runRules(patch,options);const high=findings.filter(f=>f.severity==="high").length,medium=findings.filter(f=>f.severity==="medium").length,low=findings.filter(f=>f.severity==="low").length;const without={schemaVersion:"1" as const,createdAt:"deterministic",patch:{hash:patch.patchHash,files:patch.files.length,additions:patch.additions,deletions:patch.deletions},findings,summary:{high,medium,low,verdict:(high?"block":medium?"review":"pass") as "pass"|"review"|"block"}};return{...without,reviewHash:hashCanonical(without)};}
