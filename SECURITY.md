# Security Policy

PatchLens processes patches that may contain sensitive source. It makes no network calls, but generated review artifacts can reveal repository-relative paths and change shape. Inspect them before sharing.

Report vulnerabilities through GitHub Security Advisories. Never publish live credentials or private patches in an issue. Secret findings intentionally redact matched values.
