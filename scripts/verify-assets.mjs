import {readFile} from "node:fs/promises";const r=await readFile(new URL("../README.md",import.meta.url),"utf8"),i=await readFile(new URL("../docs/assets/patchlens-hero.png",import.meta.url));const s=Buffer.from([137,80,78,71,13,10,26,10]);if(!i.subarray(0,8).equals(s))throw new Error("hero is not PNG");const h=i.indexOf(Buffer.from("IHDR")),w=i.readUInt32BE(h+4),x=i.readUInt32BE(h+8);if(w!==1536||x!==860)throw new Error(`bad hero dimensions ${w}x${x}`);if(i.length<10000)throw new Error("hero too small");if(!r.includes("docs/assets/patchlens-hero.png")||/<img[^>]+\.svg/i.test(r))throw new Error("README hero contract failed");console.log(`Hero verified: ${w}x${x}, ${i.length} bytes.`);
