import {readFile} from "node:fs/promises";const s=JSON.parse(await readFile(new URL("../schema/patchlens-review.schema.json",import.meta.url),"utf8"));if(s.$schema!=="https://json-schema.org/draft/2020-12/schema"||s.properties?.schemaVersion?.const!=="1")throw new Error("schema contract mismatch");for(const k of ["patch","findings","summary","reviewHash"])if(!s.required.includes(k))throw new Error(`missing ${k}`);console.log("Schema verified.");
