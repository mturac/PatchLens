# Contributing

Use Node.js 22+, add a failing behavioral test first, keep rules deterministic and isolated, and run `npm ci --ignore-scripts` followed by `npm run verify`. New findings need a stable code, severity rationale, bounded evidence, positive and negative tests, and no raw secret values.
